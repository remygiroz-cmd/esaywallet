@AGENTS.md

# Workflow

- Always commit, push, and merge your changes yourself — don't leave work
  sitting on a feature branch for the user to push.
- All changes should land on `main`. Open a PR if needed, then merge it
  immediately (squash) so `main` is always the source of truth.

